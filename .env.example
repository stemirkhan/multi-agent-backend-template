# Local development placeholders only.
# Do not put real secrets in this file.

APP_ENV=dev
APP_HOST=0.0.0.0
APP_PORT=8000
LOG_LEVEL=INFO

POSTGRES_DSN=postgresql+psycopg://app:app@localhost:5432/app
REDIS_DSN=redis://localhost:6379/0
JWT_SECRET=changeme-local-only
